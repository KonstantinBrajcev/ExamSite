<!DOCTYPE html>
<html class="h-100">
<head>
  <meta charset="UTF-8">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" crossorigin="anonymous">
  <link rel="stylesheet" href="/css/main.css?v=1.0">
  <script src="https://code.jquery.com/jquery-3.7.0.min.js" crossorigin="anonymous"></script>
  <script src="/js/main.js"></script>
  <title>{{ title }}</title>
</head>
<body class="d-flex flex-column h-100 bg-light">
  <div class="container">
    <header class="d-flex flex-wrap align-items-center justify-content-center justify-content-md-between py-3 mb-4 border-bottom">
      <a href="/" class="d-flex align-items-center col-md-3 mb-2 mb-md-0 text-dark text-decoration-none"></a>

    <!-- Логотип или название -->
    {% include "auth-template.twig" %}

      <!-- Навигационное меню -->
      <nav class="navbar navbar-expand-md navbar-light bg-light w-100 d-flex justify-content-center">
        <div class="container">
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
              <span class="navbar-toggler-icon"></span>
            </button>
          <div class="collapse navbar-collapse justify-content-center" id="navbarNav">
            <ul class="navbar-nav">
              <li class="nav-item">
              <a href="/" class="nav-link px-3 {{ active == 'home' ? 'active' : '' }}">Главная</a></li>
              <li class="nav-item">
              <a href="/user/index/" class="nav-link px-3 {{ active == 'users' ? 'active' : '' }}">Пользователи</a></li>
              <li class="nav-item">
              <a href="/user/form/" class="nav-link px-3 {{ active == 'add' ? 'active' : '' }}">Добавить</a></li>
              <li class="nav-item">
              <a href="/user/contact/" class="nav-link px-3 {{ active == 'add' ? 'active' : '' }}">Контакты</a></li>
            </ul>
          </div>
        </div>
      </nav>
    </header>
  </div>

<!-- Основное содержимое страницы -->
<main class="flex-shrink-0">
  <div class="container content-template">
    {% include content_template_name %}
  </div>
</main>

<!-- Футер -->
<footer class="footer mt-auto py-3 bg-secondary text-white">
  <div class="container-fluid">
    <div class="d-flex justify-content-between align-items-center">
      <p class="mb-0">Текущее время: <span id="server-time">{{ server_time }}</span></p>
      <span class="mb-0">All rights reserved. Kastett&Co. &copy;</span>
    </div>
  </div>
</footer>

<!-- Обновление времени на странице -->
<script>
  function updateTime() {
    const timeElement = document.getElementById('server-time');
    const currentTime = new Date();
    let hours = currentTime.getHours().toString().padStart(2, '0');
    let minutes = currentTime.getMinutes().toString().padStart(2, '0');
    let seconds = currentTime.getSeconds().toString().padStart(2, '0');
    const timeString = `${hours}:${minutes}:${seconds}`;
    timeElement.textContent = timeString;}
  setInterval(updateTime, 1000);
</script>
</body>
</html>